# AI REXI — BÁO CÁO AUDIT TOÀN DIỆN + HANDOFF (20/9/2026)

> Bản bàn giao: mọi thứ cần biết để làm tiếp dự án nếu agent/dev cũ không còn.
> Đã audit: đọc ~18.000 dòng code (FE + BE), bắn API live, điều khiển browser thật
> trên https://ai-rexi-app.vercel.app bấm từng chức năng, nghe thử TTS từng giọng,
> đánh sập video render thật (502) rồi chờ hồi phục.

---

## 1. BỐI CẢNH HỆ THỐNG

| Thành phần | Giá trị |
|---|---|
| Frontend | React + Vite + Tailwind, deploy Vercel: https://ai-rexi-app.vercel.app |
| Backend | Express 5, deploy Render free (runtime: node, KHÔNG Docker): https://ai-rexi-backend.onrender.com |
| DB | Supabase Postgres (session pooler IPv4) qua adapter `Backend/src/config/db.js` (SQLite local / PG prod) |
| Deploy BE | `render.yaml` — buildCommand: `cd Backend && npm install --omit=dev && npx playwright install chromium && cd ../Frontend && npm install --include=dev && npm run build`, start: `cd Backend && node server.js` |
| Auth | JWT 7 ngày (`rexi_token` localStorage) + express-session SQLite store |
| Key API providers | Bảng `khoa_api`, mã hóa `cryptoKeys.js` (ENC_KEY fallback từ JWT_SECRET) |
| Monorepo | `AI REXI/Frontend`, `AI REXI/Backend`, `AI REXI/skills/`, `AI REXI/tools/VieNeu-TTS` |

Cấu trúc chính:
- FE: `Frontend/src/App.jsx` (2219 dòng — chat, FAB, auth modal, theme), `components/` (ChatTab, StudioTab, GameTab, IPTVTab, VideoCreatorTab, YouTubeTab, AdminPanel.jsx 1918 dòng ở `src/AdminPanel.jsx`…)
- BE routes: `Backend/src/routes/` (chat, services, models, admin, auth, github, media, workspace, agent, realtimeTranscription)
- BE services: `modelRouter` (định tuyến model + fallback), `chatExecutor` (gọi provider + retry), `quotaManager` (throttle 429), `intentRouter` (câu → tab), `internalAgent` (ReAct), `edgeTTS`, `videoRenderer`, `skillRouter`, `ragService`, `brain/` (context engine)
- Session Guest: quota 10 tin nhắn lưu trong express-session (SQLite store)

---

## 2. ĐÃ SỬA TRONG SESSION NÀY (chưa commit / chưa deploy)

1. **VieNeu-TTS v3** — wire vào `Backend/src/routes/services.routes.js`: engine `vieneu` (server OpenAI-compatible `POST /v1/audio/speech` chạy local `tools/VieNeu-TTS`, `uv run vieneu-server`), `GET /tts/voices` trả 25 giọng khi engine bật, fallback Edge khi chết. Test local: sinh audio 407KB OK. **Chưa deploy** (Render free không chạy nổi model — cần chạy local hoặc key vieneu.io v4).
2. **HyperFrames 0.7.94 → 0.8.54** — nâng trong `Backend/package.json` (bug CDP "Network.enable timed out" đã được upstream sửa; render thử local 300/300 frame OK 25s). **Chưa commit/deploy.**
3. **YouTube: ĐỔI SANG youtube-dl-exec (npm) — XONG + TEST OK.**
   - Bỏ hẳn phụ thuộc Python + `ytdlp_helper.py` (đã xóa file helper).
   - `Backend/src/services/ytdlpService.js` viết lại bằng `youtube-dl-exec` v3.1.15, giữ nguyên API export (`searchVideos`, `getVideoStream`, `downloadAudio`, thêm `getStatus`) + shape field cũ để routes/FE không phải sửa.
   - Binary yt-dlp tự tải vào `node_modules/youtube-dl-exec/bin/` lúc `npm install` (postinstall). Local test: STATUS ready=true (yt-dlp 2026.08.19), SEARCH OK, STREAM OK (360p mp4), AUDIO OK (mp3 152KB).
   - `/api/services/youtube/status` sửa: check binary thay vì probe Python.
   - **2 bẫy đã gặp — AI khác đừng dính lại:**
     a. youtube-dl-exec v3 KHÔNG có option `timeout` (đẩy `--timeout` cho yt-dlp → lỗi "no such option"). Dùng `socketTimeout` + wrapper `withTimeout` tự viết.
     b. `dumpJson: true` → `--dump-json` của yt-dlp ÉP SIMULATE MODE → không tải file nào. Bắt buộc dùng `printJson: true` (in JSON sau khi tải) rồi tự parse dòng JSON cuối stdout.
   - **Deploy Render:** postinstall phải chạy để tải binary. Nếu npm chặn install scripts → binary không có → `/youtube/status` trả `ready=false`. Kiểm tra sau deploy bằng endpoint này.

---

## 3. TOÀN BỘ 85 VẤN ĐỀ CÒN LẠI — CHI TIẾT TỪNG CÁI

Format: **Vị trí** — Hiện tượng. Gốc. → Cách sửa.

### Nhóm A — THEME SÁNG/TỐI (11)

A1. **`Frontend/src/App.jsx` khối FLOATING SPEED DIAL** — Bật dark → menu FAB nền trắng chói 563px (đo computed style `rgb(255,255,255)`), chính là lỗi "chế độ tối có chỗ nền trắng". Class `bg-white border-slate-200` cứng. → Đổi `bg-[#181920] border-white/10` (2 class có sẵn remap sáng trong index.css).

A2. **Cùng menu FAB đó** — Menu ghép từ 2 bản: nhóm "Trí Tuệ AI/Sáng Tạo/Giải Trí" viết kiểu light (`text-slate-600 hover:bg-slate-50`), nhóm "Trợ Giúp/35+ Skills/Super Tools/Cài Đặt" viết kiểu dark (`text-slate-300 hover:bg-white/10`). Light mode → nhóm sau chữ sáng trên nền trắng gần vô hình; dark mode → nhóm trước pastel lệch tông. → Chuẩn hóa 1 FabItem component dùng chung token `var(--text-main)` / `var(--bg-card)`.

A3. **FabItem active** — `bg-cyan-50 text-cyan-600 border-cyan-200` pastel sáng cứng, không theo theme. → Đổi `bg-cyan-500/10 text-cyan-300` (được remap sáng).

A4. **Overlay sau FAB menu** — inline `rgba(0,0,0,.08)`: dark mode gần như không có màn che, menu lơ lửng. → `bg-black/40` + remap light sang đen nhạt.

A5. **`App.jsx` khối USER AUTH MODAL** — dark mode: tab switcher `bg-white border-slate-200`, nút inactive `text-slate-600 hover:bg-slate-50`, input `bg-slate-50 border-slate-200 text-slate-800`, divider `bg-slate-200`, nút Google `bg-white text-slate-800` — khối trắng chữ đen chói giữa modal `#181920`. Lỗi "tối mà nền trắng chữ đen" user chụp. → Toàn bộ input/tab/nút đổi `bg-[#131417] border-white/10 text-slate-200`.

A6. **`StudioTab.jsx` root** — TTS Studio TOÀN tab nền trắng cứng: root `bg-white`, header/border/samples/cards/sliders/player/history `bg-slate-50 border-slate-200 text-slate-800`; nghịch lý: textarea `bg-[#12131a]` (tối) nằm giữa panel trắng — 1 tab 2 tông đối lập. → root `bg-[var(--bg-main)]`, panel `var(--bg-card)`, border `border-white/5`.

A7. **History TTS** — `group-hover:text-white` trên card `bg-white`: rê chuột chữ trắng trên nền trắng, text biến mất. → `group-hover:text-[var(--text-main)]`.

A8. **`OpenCutTab.jsx`** — trắng cứng: `bg-white`, toolbar `bg-slate-50`, input `bg-white border-slate-200`. Dark vỡ toàn tab. → Sửa như A6.

A9. **`DocumentsTab.jsx`** — trắng cứng: `text-slate-800`, upload box `border-slate-300`, error `bg-red-50`, note `bg-slate-50`. → Sửa như A6.

A10. **`Frontend/src/index.css`** — phần remap light chỉ remap được `#1e1f20, #181920, #13141c, #141522, #1a1b24, #16171f`; thiếu `#0d0e14, #10111a, #12131a, #0b0c10` → light mode vẫn còn mảng tối lọt lại. → Bổ sung đủ bảng remap.

A11. **Icon Sun/Moon (header)** — animation xoay đẹp nhưng Sun vàng nhạt/Moon tím trên nền `#131417`, khó phân biệt đang sáng hay tối. → Đổi màu icon theo state + stroke dày hơn.

### Nhóm B — NÚT MỞ THANH CÔNG CỤ (FAB) (3)

B1. **FAB button** — tia sét (Zap) tròn 48px màu `#4a7dff` sát góc dưới phải; không diễn đạt "công cụ". → Icon Wrench/Sliders, chừa margin, tooltip "Công cụ".

B2. **Menu không hover-tooltip** — animation đẩy menu lên có, nhưng rê vào từng công cụ không hiện gì thêm. → Thêm tooltip title/desc hover từng item.

B3. **Mô tả công cụ chữ 9px** — nhòe không đọc được. → 11–12px + `line-clamp`.

### Nhóm C — i18n EN/VI (3)

C1. **`Frontend/src/i18n.js` chỉ có 13 key** — cả app hardcode ~200+ chuỗi tiếng Việt. Bấm EN chỉ đổi placeholder ô chat + vài nhãn (đếm live: "Cuộc Trò Chuyện Mới", "Đăng nhập", "Tạo Video", "Xem TV"… vẫn VI). → Trích chuỗi + thêm key.

C2. **Sidebar/FAB/modals/tabs không dùng `t()`** — toàn bộ component lớn không map key. → Ưu tiên header/sidebar/FAB trước, rồi modal.

C3. **Cảm giác "đổi mỗi dòng"** — hệ quả C1/C2, không phải lỗi state.

### Nhóm D — GAME (2)

D1. **Game Pac-Man embed passer-by.com** — live đọc được chữ Trung trong iframe: `项目/文字/摄影/留言/关于/按 [空格键] 暂停或继续/吃豆人游戏…` kèm logo + footer trang người ta. → Việt hóa wrapper (title/hướng dẫn của mình), che chrome iframe, hoặc tìm bản khác.

D2. **Không có khung wrapper chung cho games** — iframe nhúng trần, không loading state, không fullscreen, không báo khi bị chặn. Snake/Pong OK không Trung. → Viết GameFrame chung.

### Nhóm E — ĐĂNG NHẬP + KHIÊN ADMIN (5)

E1. **`Sidebar.jsx` line ~230** — icon khiên Shield render cho MỌI người kể cả guest; bấm mới báo "Vui lòng đăng nhập". → Ẩn khi `!currentUser`, chỉ hiện khi `phan_quyen === 'admin'`.

E2. **Nút Đăng nhập** — không tooltip (`title=null` xác minh live), user chê "rê chuột vào không hiện thông tin". → Thêm title/tooltip.

E3. **Guest xóa hội thoại** — bấm xóa KHÔNG có gì xảy ra (401 nuốt vào catch rỗng, không toast, hội thoại không mất). → Bắt lỗi + toast "Đăng nhập để xóa".

E4. **Login xóa hội thoại người khác** — 403 cũng im lặng hoàn toàn. → Bắt lỗi 403 + toast.

E5. **Backend soft-delete** (`chat.routes.js` set `ngay_xoa`) — "xóa" vẫn nằm DB mãi, không có UI thùng rác/xóa vĩnh viễn. → Cron purge 30 ngày + (tuỳ) tab thùng rác admin.

### Nhóm F — TTS GIỌNG NÓI (6)

F1. **`StudioTab.jsx` hardcode 10 giọng** — Edge TTS thực tế chỉ còn 2 sống (Hoài Mỹ, Nam Minh). Test live: 8/10 giọng còn lại trả audio byte GIỐNG HẲN Hoài Mỹ.

F2. **Backend âm thầm thay giọng** — chọn giọng chết → fallback Hoài Mỹ không báo. → Trả lỗi rõ "giọng không khả dụng".

F3. **FE không fetch `/tts/voices`** — không biết giọng nào sống. Endpoint đã có; StudioTab phải bỏ hardcode, fetch list theo engine.

F4. **TTS fail im lặng với guest** — fetch 401 không toast.

F5. **VieNeu đã wire backend, FE chưa dùng** — xem mục 2.1. Render free không chạy model: chỉ dùng local, hoặc mua key hosted vieneu.io (v4).

F6. **Lịch sử/player không phân biệt format** — VieNeu trả wav, Edge trả mp3; play/download phải theo `Content-Type` response.

### Nhóm G — AGENT MODE (4)

G1. **Agent Mode chết trên cloud** — bấm → "⛔ dsh chưa được cài đặt". Engine `auto/dsh` trỏ CLI OpenCode (`opencode.exe`) chỉ có trên máy local; Render không có binary. Test live cả auto lẫn dsh đều chết.

G2. **UI vẫn quảng cáo Agent Mode** — không ẩn/hướng dẫn khi backend không có engine cloud. → Ẩn tab hoặc banner "chỉ chạy bản local".

G3. **Khả năng agent (đọc link MXH, search web…) treo theo engine chết** — social/web-search chạy trong `agentEngine.js`.

G4. **`internalAgent.js` có sẵn nhưng không fallback** — agent.routes chết hẳn thay vì chuyển qua internalAgent (ReAct qua chat API, không cần opencode.exe — đã cấu hình AGENT_PROVIDER=bai trong render.yaml). → Wire fallback.

### Nhóm H — YOUTUBE (3) — [H1 ĐÃ SỬA XONG, xem mục 2.3]

H1. ~~YouTube Free chết trên prod — thiếu yt_dlp Python trên Render~~ → **ĐÃ SỬA**: youtube-dl-exec npm, không cần Python. Còn lại chỉ là deploy + verify `/youtube/status` sau khi push.

H2. **FE chưa dùng `/youtube/status` để ẩn tab** — tab luôn hiện; khi engine chết user chỉ thấy lỗi sau khi bấm. → Fetch status khi mở tab; `ready=false` → ẩn + banner.

H3. **Tải audio/tóm tắt AI cùng service** — đã sống lại theo H1; verify `/youtube/summarize` sau deploy.

### Nhóm I — VIDEO RENDER (5)

I1. **Render video làm sập cả service Render free** — OOM → 502 toàn app (xác minh live, đợi ~1 phút hồi phục). Frame-by-frame 10s@30fps = 300 screenshot + ffmpeg, RAM 512MB không đủ.

I2. **Không hàng đợi** — request nào cũng chạy ngay; nhiều user render cùng lúc = chết. → Queue đơn giản (mảng + 1 worker) hoặc BullMQ.

I3. **Không giới hạn duration/resolution trên free** — 30s@1080p là đủ giết. → Cap 10s/720p trên prod, bỏ cap khi chạy local.

I4. **Engine cũ hyperframes 0.7.94 chết CDP** — đã nâng 0.8.54 (mục 2.2), `videoRenderer.js` tự viết Playwright+ffmpeg vẫn đang là engine chính. → Sau deploy thử lại hyperframes, nếu ổn thay videoRenderer.

I5. **File render nằm `Backend/temp/` 1h + container ephemeral** — link tải chết sau restart; FE không xử lý file mất. → Thông báo hết hạn + (tuỳ) upload storage.

### Nhóm J — VỆ SINH CODE (5)

J1. **`Frontend/src/AdminPage.jsx` 641 dòng DEAD CODE** — không file nào import; bản duplicate cũ của AdminPanel; theme tự mâu thuẫn (root `bg-slate-50` sáng, card `#181920` tối). → Xóa.

J2. **Label "D:\" lộ trong UI/copy** — rò rỉ máy local. → Grep "D:\" toàn FE, thay text trung tính.

J3. **Encoding mojibake hàng loạt file backend** — `server.js`, `models.routes.js`… comment dạng `cÃ¡c routes`, `Ä‘Ã£` (UTF-8 đọc nhầm Latin-1). Sửa code sau này cực dễ gãy. → Đọc lại file đúng encoding, lưu UTF-8 thuần.

J4. **Console.log dư** — `[AdminPanel] Triggering scan...` v.v mỗi lần bấm; log lỗi EN/VI không thống nhất. → Dọn hoặc thống nhất qua 1 logger.

J5. **Chữ Trung lọt code** — `quotaManager.js` log `恢复正常`, comment `出局`; các file khác cũng lọt (mới gặp ở comment tự viết — luôn soát). → Grep `\p{Han}` toàn repo, dịch lại.

### Nhóm K — ADMIN PANEL (10)

K1. **`Frontend/src/main.jsx` — URL `/admin` chết hẳn** — `{isAdmin ? <AdminPanel /> : <App />}` render `<AdminPanel />` KHÔNG props → useEffect thấy `!token` → `window.location.href='/'` → gõ `…vercel.app/admin` bị đá về trang chủ ngay. Chỉ vào admin được qua nút khiên sidebar. → Cho AdminPanel tự đọc token/user từ localStorage khi thiếu props.

K2. **Trùng lặp admin UI** — AdminPanel.jsx (dùng) vs AdminPage.jsx (dead). Giữ 1 (xem J1).

K3. **Dropdown "Nvidia NIM/Cerebras/Cohere/Mistral" LỘ API KEY** — `Backend/src/routes/models.routes.js fetchModelsFromProvider` KHÔNG có nhánh cho 4 provider này → rơi nhánh `custom` → base mặc định `https://openrouter.ai/api/v1` → key NVIDIA/Cerebras… gửi làm Bearer thẳng tới openrouter.ai (quét fail + credential rơi cho host khác). Ngược lại backend hỗ trợ `xkiro/agentrouter/bai/kiosapi/unorouter` mà UI không có option. → Thêm nhánh riêng từng provider + đồng bộ dropdown `AdminPanel.jsx` (list 14 option hiện tại).

K4. **Quét model xóa sạch model cũ** — `scan-provider`, `verify-and-scan`, `publish-active` đều `DELETE FROM ai_models WHERE ma_nha_cung_cap=?` trước khi lưu → model đã thủ công bật/tắt (`kich_hoat`) mất hết mỗi lần quét. → Upsert + giữ `kich_hoat` cũ.

K5. **Terminal Exec (`SettingsTab` → `/chat/exec`)** — chạy lệnh shell BẤT KỲ trên server qua HTTP, chốt chặn duy nhất header `X-Exec-Confirm`. Token admin lộ = mất server. → Whitelist lệnh + confirm lần 2 + audit hiển thị.

K6. **`AdminChatTab`** — báo "hội thoại mới" chỉ so tổng số conv (xóa conv không phát hiện); messages không phân trang; không filter user.

K7. **`catch {}` rỗng hàng loạt** (SkillsTab, notifications, markRead…) — lỗi mạng UI đứng im không báo.

K8. **IPTV "Quét kênh" không khóa** — mỗi bấm `POST /admin/scan-now` spawn thêm 1 process `node scripts/scan_full.js`; bấm 3 lần = 3 process quét song song → giết RAM Render. → Chống double-run (check `iptv_scan_log status='running'`).

K9. **`RoutingTab` bar % sai** — mốc 100% lấy `providers[0]?.count` (provider đầu, không phải max) → bar các provider khác phóng đại/kẹp sai.

K10. **`h-13`** — class Tailwind không tồn tại (AdminPanel header). → `h-12`/`h-14`.

### Nhóm L — AUTH BACKEND (7)

L1. **Quên mật khẩu CHẾT trên prod** — `Backend/src/routes/auth.routes.js forgot-password`: OTP chỉ `console.log`, KHÔNG gửi email (nodemailer có sẵn trong package.json nhưng không dùng); prod không trả `otp_debug` → user không bao giờ nhận được OTP. → Gửi mail thật qua nodemailer (SMTP env).

L2. **OTP brute-force được** — plaintext DB, 6 số, hạn 10 phút, KHÔNG đếm số lần thử. → Hash OTP + counter + lock.

L3. **Register lỏng** — ô "Tài khoản" làm `email` luôn (đăng ký `tuan` → email=`"tuan"`); không validate format; mọi lỗi DB trả chung "Email này có thể đã tồn tại"; không set `trang_thai` mặc định. → Validate + tách account/email + error đúng.

L4. **Login match theo prefix** — `WHERE LOWER(email)=? OR LOWER(email)=? OR LOWER(email) LIKE account+'@%'`: gõ `a` match mọi email `a@…`; trùng password là login nhầm account người khác. → Bỏ LIKE, hỗ trợ nickname bằng cột riêng.

L5. **Google OAuth leak JWT vào URL** — callback redirect `?google_token=…&user=…` → nằm trong browser history/server log/Referer; FE không thấy code dọn URL. → Đổi code ngắn hạn qua POST, hoặc hash-fragment, hoặc FE dọn `history.replaceState`.

L6. **Logout giả** — chỉ xóa localStorage; JWT 7 ngày vẫn hợp lệ, không revoke/blacklist.

L7. **Không guard "ít nhất 1 admin"** — các admin hạ quyền lẫn nhau → mất hết admin. → Chặn hạ admin cuối cùng.

### Nhóm M — BACKEND CORE (7)

M1. **GỐC THẬT lỗi "nói mở truyền hình → nhầm tab"** — `Backend/src/services/intentRouter.js`: regex chứa mẫu CÓ DẤU (`truyền hình`, `xem trực tiếp kênh`) nhưng text đã stripDiacritics → mẫu CHẾT vĩnh viễn không match; nhóm `mo (kenh|tv|tivi)` thiếu biến thể `mo truyen hinh` → "mở truyền hình trực tuyến" rơi xuyên, kẹt intent khác. → Viết lại pattern không dấu chuẩn + đủ biến thể.

M2. **intentRouter overmatch** — `channels?` bắt "channel" trong câu Anh; `draw ` bắt "withdraw"; `game` bắt trong từ bất kỳ; confidence `none` vẫn trả match → FE chuyển tab oan. → Word boundary + ngưỡng confidence.

M3. **`quotaManager.js` in-memory thuần** — Render restart/cold-start mất sạch throttle + usage → backoff 429 vô nghĩa sau mỗi lần ngủ. → Lưu DB (mẫu: `sqliteSessionStore.js`).

M4. **`modelRouter.js` ROUTES hardcode model id** (`nemotron-3.5-lightning-free`, `big-pickle`, `claude-opus-4-8`, `gpt-5.6-sol`…) — provider đổi model là route chết; live discovery chỉ check tồn tại, không re-rank.

M5. **classify sai loại câu** — vision pattern `anh |hinh anh` bắt cả "một anh bạn"; math `tinh ` (có dấu cách) bắt "tinh thần"; `hasImage` chỉ regex trong text, không xem ảnh thật. → Sửa pattern + nhận attachment thật.

M6. **Timeout ăn throttle oan** — câu deep >60s bị AbortSignal cắt → vẫn `recordThrottle` phạt provider 30s dù provider không lỗi.

M7. **History re-send base64 cũ mỗi lượt** — `chatExecutor.js buildOpenAIContent` regex markdown-image trên TOÀN history, không trim → payload phình, chậm, đốt token. → Trim history + co ảnh cũ.

### Nhóm N — INFRA / FILE / MEDIA (10)

N1. ~~Encoding backend~~ = J3 (tính 1 lần khi sửa).

N2. **helmet CSP chặn iframe game** — `server.js frameSrc` chỉ cho `youtube.com` + `accounts.google.com`; nếu FE serve từ backend (same-origin) thì Game Zone trắng trang. Vercel host riêng nên chưa dính; backup-domain same-origin dính. → Thêm host game vào frameSrc.

N3. **User thường đọc được SOURCE CODE** — `Backend/src/routes/workspace.routes.js GET /file-content` chỉ cần `authMiddleware` (không admin); chặn `.env/.db/.log` nhưng `.sql/.bak` + mọi file khác đọc được toàn workspace. → Admin-only, hoặc whitelist thư mục.

N4. **`POST /admin/scan-now` fire-and-forget** — `exec` không track process, timeout 10 phút ngầm, không chống double-run (= K8).

N5. **`import-sqlite` dùng ADMIN_PASSWORD làm bootstrap_key** — DB trống + biết password admin là import đè DB. → Tách biến riêng IMPORT_BOOTSTRAP_KEY.

N6. **`export-db` dump cả `sessions_store` + `otp_code` plaintext** — backup rơi nhầm = lộ phiên đăng nhập + OTP. → Mask/loại bảng nhạy cảm khi export.

N7. **`media.routes.js`** — `/speech` cắt text 4000 ký tự IM LẶNG (`String(text).slice(0,4000)` không báo); `/image` trả `response_format:'url'` — URL upstream hết hạn nhanh, history lưu URL là chết ảnh. → Trả base64 hoặc cache ảnh server.

N8. ~~yt-dlp phụ thuộc Python~~ → ĐÃ SỬA (youtube-dl-exec).

N9. **`videoRenderer.js` không queue/limit** — gốc 1 chỗ với I1–I3: thêm hàng đợi + cap.

N10. ~~hyperframes 0.8.54 chưa deploy~~ — xem mục 2.2.

### Nhóm O — GITHUB TRENDING (tab admin) (4)

O1. **Polling vô hạn** — `GitHubTrending.jsx refreshAll` mỗi 60s + refresh khi quay lại tab → 4 API/phút liên tục, đốt giờ Render free. → Chỉ polling khi tab đang mở + backoff.

O2. **Star check chỉ 30 repo đầu** (`slice(0,30)`) — repo ngoài top 30 không biết trạng thái star, bấm star có thể trùng. → Check theo repo đang hiển thị.

O3. **Export lỗi chỉ setError banner chung** — không toast, không loading.

O4. **Đọc mô tả bằng speechSynthesis voice EN** — máy thiếu voice EN thì im lặng, không báo.

---

## 4. THỨ TỰ SỬA ĐỀ XUẤT

- **Đợt 1 (sống còn + an toàn):** L1–L7 (auth), K3 (lộ key), F1–F4 (TTS) + deploy VieNeu/YouTube/hyperframes lên Render.
- **Đợt 2 (UI/UX user chê):** A1–A11, B1–B3, E1–E4.
- **Đợt 3 (logic chết):** M1–M2 (intent), G1–G4 (agent), N9+I1–I3 (video queue), C1–C2 (i18n), còn lại theo bảng.

## 5. CÁCH TEST LẠI SAU KHI SỬA

```bash
# Health + login admin (đổi user/pass thật)
curl -s -m 90 https://ai-rexi-backend.onrender.com/api/health
TOKEN=$(curl -s -m 60 -X POST https://ai-rexi-backend.onrender.com/api/auth/login \
  -H 'Content-Type: application/json' -d '{"account":"admin","password":"…"}' | python -c "import sys,json;print(json.load(sys.stdin)['token'])")

# YouTube (sau deploy youtube-dl-exec): ready phải = true
curl -s -H "Authorization: Bearer $TOKEN" https://ai-rexi-backend.onrender.com/api/services/youtube/status
curl -s -H "Authorization: Bearer $TOKEN" "https://ai-rexi-backend.onrender.com/api/services/youtube/search?q=test&limit=2"

# TTS từng giọng — so sánh MD5 2 giọng khác nhau phải KHÁC nhau
# Intent router: "mở truyền hình trực tuyến" phải trả tab=iptv
# Theme: bật dark → đo computed style FAB menu phải KHÔNG phải rgb(255,255,255)
```

Browser thật (đã dùng trong audit): Chrome với `--remote-debugging-port=9222`, CDP WebSocket, chụp DOM/computed style, click từng nút trên Vercel app.

## 6. LƯU Ý DEPLOY

- Render free: cold start ~50s, RAM 512MB — video render + IPTV scan là 2 thứ dễ giết service.
- `render.yaml` buildCommand KHÔNG cài python/yt-dlp hệ thống — youtube-dl-exec mới không cần; đảm bảo install scripts không bị chặn.
- FE deploy Vercel tự chạy từ repo; BE phải push cả `package.json` (hyperframes 0.8.54 + youtube-dl-exec) + `ytdlpService.js` + `services.routes.js`.
- VieNeu-TTS KHÔNG chạy được trên Render free — giữ local-only hoặc mua key hosted.
